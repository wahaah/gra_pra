<?php
    // print_r($_FILES['file']);
    // $file = $_FILES['file'];
    // // 把文件保存到指定目录
    // // 获取后缀名
    // // strchr（"字符串"，符号）
    // $ext = strrchr($file['name'],".");
    // echo $ext;
    // $fileName = time().rand(10000,99999).$ext;
    // $fileName = time().rand(1000,9999).$ext;
	// echo $fileName;
    // // 把文件保存到指定目录
	// move_uploaded_file($file['tmp_name'],"../../bishe/uploads/". $fileName );
?>